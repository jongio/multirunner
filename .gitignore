# Local config holding secrets / machine-specific settings
config.local.yaml
config.yaml
config.qemu-run.yaml

# Secrets
*.pem

# Cache / mirror data dirs
/cache/
/gitmirror/

# VM build artifacts (ISOs, golden images, firmware/NVRAM)
/vmbuild/
*.iso
*.qcow2
*.vars.fd
*.fd
*.serial.log

# Build output (repo-root binaries only — leading slash so the cmd/multirunner
# and cmd/cacheserver source packages are NOT ignored)
/dist/
/bin/
/multirunner
/multirunner.exe
/multirunner-linux
/cacheserver
/cacheserver.exe

# Logs
*.log

# OS / editor
.DS_Store
*.swp

# Claude / agent state
.claude/

# more binaries
/cacheserver-linux
/cacheserver-*

# dotenv (secrets)
.env
